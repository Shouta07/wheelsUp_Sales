# Supabase REST (omit to run in mock mode)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite

# Jina Reader (optional; falls back to plain fetch)
JINA_API_KEY=

# Shared secret for all /api endpoints — pass as ?secret=...
CRON_SECRET=change-me
