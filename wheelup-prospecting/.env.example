# --- Supabase (omit to run in mock mode using data/*) ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- Gemini ---
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash-lite

# --- Jina Reader (optional; fallback to plain fetch when empty) ---
JINA_API_KEY=

# --- API auth (single shared secret for cron + manual triggers) ---
CRON_SECRET=replace-me
